include ./mixins/views_mixins_forms.jade

include ./common/views_html5.jade

include ./common/views_header

h3( class = 'title' ) #{ heading }

div( class = 'panel panel-warning' )

  div( class = 'panel-heading' )

    p( class = 'panel-title' ) Presently no input validation installed

  form( action = '/admin/users/new', method = 'post' )

    div( class = 'row' )

      div( class = 'col-lg-6' )

        mixin field( 'text', 'email', 'Email' )

        mixin field( 'text', 'name', 'Name' )

        p An email will be sent prompting the user to create their password.

        mixin button_submit( 'Submit' )

include ./common/views_footer